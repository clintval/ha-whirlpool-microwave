[project]
name = "ha-whirlpool-microwave"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
test = [
    "pytest",
    "pytest-homeassistant-custom-component",
    "whirlpool-sixth-sense==1.0.3",
]
